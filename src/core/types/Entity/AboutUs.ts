export interface AboutUsEntity {
    email: string;
    id?: string;
    image: string;
    sections: Array<string>;
    skills: Array<string>;
    tagId: string;
    title: string;
    youtubeLinks: Array<string>;
}
