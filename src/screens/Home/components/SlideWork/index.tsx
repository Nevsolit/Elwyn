import { WrapperSection } from "~/core/components";

import "./styles.scss";

const SlideWork: React.FC = () => {
    return (
        <WrapperSection>
            <div className="slide__work__container"></div>
        </WrapperSection>
    );
};

export default SlideWork;
